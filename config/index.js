require('dotenv').config();

let config = {
    host: process.env.HOST,
    db_name: process.env.DB_NAME,
    host: process.env.HOST,
    host: process.env.HOST
}

module.exports = { config , api , sockets}

